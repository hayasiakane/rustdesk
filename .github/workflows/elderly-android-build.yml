name: Elderly-Friendly Android APK Build

on:
  push:
    branches: [ main, master ]
    paths-ignore:
      - 'docs/**'
      - 'res/**'
      - '*.md'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'release'
        type: choice
        options:
        - release
        - debug

env:
  FLUTTER_VERSION: "3.24.5"
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"
  JAVA_OPTS: "-Xmx4g -XX:+UseG1GC"

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25b

      - name: Cache cargo-vcpkg
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-vcpkg-${{ hashFiles('**/Cargo.lock') }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libasound2-dev \
            libxcb-dev \
            libxdo-dev \
            libxfixes-dev \
            libxcb1-dev \
            libx11-dev \
            libxext-dev \
            libxrandr-dev \
            libxcursor-dev \
            libxdamage-dev \
            libxinerama-dev \
            libgtk-3-dev

      - name: Setup vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg ~/vcpkg
          cd ~/vcpkg
          ./bootstrap-vcpkg.sh
          echo "VCPKG_ROOT=$HOME/vcpkg" >> $GITHUB_ENV
          echo "$HOME/vcpkg" >> $GITHUB_PATH

      - name: Install vcpkg ports
        run: |
          $VCPKG_ROOT/vcpkg install libvpx:arm64-android libvpx:arm-android libvpx:x64-android
          $VCPKG_ROOT/vcpkg install libyuv:arm64-android libyuv:arm-android libyuv:x64-android
          $VCPKG_ROOT/vcpkg install opus:arm64-android opus:arm-android opus:x64-android
          $VCPKG_ROOT/vcpkg install aom:arm64-android aom:arm-android aom:x64-android

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --version=3.2.0

      - name: Install flutter_rust_bridge_codegen
        run: |
          cargo install flutter_rust_bridge_codegen --version=2.0.0-dev.32 || true

      - name: Generate bridge files
        run: |
          cd flutter
          flutter pub get
          ~/.cargo/bin/flutter_rust_bridge_codegen generate

      - name: Build Rust library for Android
        run: |
          cargo ndk -t armeabi-v7a -t arm64-v8a -t x86_64 \
            -o flutter/android/app/src/main/jniLibs build --release

      - name: Build Flutter APK
        run: |
          cd flutter
          flutter pub get
          flutter build apk --${{ inputs.build_type || 'release' }} --split-per-abi

      - name: Rename APK files
        run: |
          cd flutter/build/app/outputs/flutter-apk
          mv app-armeabi-v7a-${{ inputs.build_type || 'release' }}.apk rustdesk-elderly-arm32.apk
          mv app-arm64-v8a-${{ inputs.build_type || 'release' }}.apk rustdesk-elderly-arm64.apk
          mv app-x86_64-${{ inputs.build_type || 'release' }}.apk rustdesk-elderly-x86_64.apk
          cp app-${{ inputs.build_type || 'release' }}.apk rustdesk-elderly-universal.apk

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: elder-android-apk-${{ inputs.build_type || 'release' }}
          path: |
            flutter/build/app/outputs/flutter-apk/*.apk
          retention-days: 90

      - name: Upload APK to Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: elderly-build-${{ github.run_number }}
          files: |
            flutter/build/app/outputs/flutter-apk/rustdesk-elderly-*.apk
          name: "Elderly-Friendly APK Build ${{ github.run_number }}"
          body: |
            专为老年用户优化的远程协助软件
            - 内置服务器：223.26.59.132
            - 一键获取所有权限
            - 大号按钮和清晰界面
          prerelease: false